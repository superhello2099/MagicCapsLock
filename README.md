# MagicCapsLock: Unlock Your Keyboard's Hidden Magic Power

[English](#magiccapslock-unlock-your-keyboards-hidden-magical-power) | [中文](#magiccapslock---capslock键盘魔法师让capslock键化身超级生产力工具)

**Transform your CapsLock key from rarely-used annoyance to productivity supercharger.**

Are you tired of:
- Accidentally hitting CapsLock and TYPING LIKE THIS?
- Reaching for distant keyboard shortcuts that strain your hands?
- Constantly switching between keyboard and mouse for navigation?
- Performing repetitive text editing operations?

This lightweight script gives you 30+ powerful shortcuts at your fingertips without changing your keyboard's layout or learning complex key combinations.

**Simple to use**: Press CapsLock+F1 to see all commands, no memorization needed.

**Zero learning curve**: Start with basics like navigation, gradually discover more features as you need them.

**Keeps original function**: Triple-tap CapsLock to toggle caps - the proper way it should work!

## Features

### Core Functionality
- CapsLock becomes a modifier key for 30+ custom shortcuts
- Triple-press CapsLock to toggle traditional CapsLock state

### Navigation
- Vim-like cursor movement (h/j/k/l style with different keys)
- Quick word/line/page navigation
- Efficient text selection

### Text Editing
- Delete words, lines, and characters with ease
- Case conversion (lowercase, UPPERCASE, Title Case)
- Indentation control

### Window Management
- Toggle window always-on-top
- Minimize current window

### Additional Features
- Quick Google search of selected text
- Perplexity AI quick access
- Date insertion
- Built-in help system (Caps+F1)

## Quick Start Guide

1. **Install AutoHotkey**:
   - Download from [AutoHotkey Official Site](https://www.autohotkey.com/download/)
   - Run the installer and follow the prompts (default options are fine)
   - This script is compatible with AutoHotkey v1.x

2. **Get the Script**:
   - Download `capslock_enhanced.ahk` from this repository
   - Save it anywhere on your computer

3. **Run the Script**:
   - Double-click the script file to run it immediately
   - A small "H" icon will appear in your system tray showing the script is active

4. **Auto-start with Windows** (recommended):
   - Press `Win+R`, type `shell:startup` and press Enter
   - Copy or create a shortcut to the script in this folder
   - The script will now run automatically each time you start your computer

## Usage

Press **CapsLock + F1** at any time to see all available shortcuts.

### Key Shortcuts Overview

#### Cursor Movement
- `CapsLock + f` → Right
- `CapsLock + b` → Left
- `CapsLock + u` → Up
- `CapsLock + n` → Down
- `CapsLock + p` → Word Right
- `CapsLock + o` → Word Left
- `CapsLock + a` → Home
- `CapsLock + e` → End
- `CapsLock + t` → Page Up
- `CapsLock + v` → Page Down

#### Text Editing
- `CapsLock + h` → Backspace
- `CapsLock + d` → Delete
- `CapsLock + k` → Delete Word Right
- `CapsLock + j` → Delete Word Left
- `CapsLock + l` → Delete to End
- `CapsLock + x` → Cut Line
- `CapsLock + [/]` → Indent Left/Right

#### Selection & Formatting
- `CapsLock + q` → Selection Mode
- `CapsLock + i` → Select Current Line
- `CapsLock + .` → To UPPERCASE
- `CapsLock + ,` → To lowercase
- `CapsLock + /` → Capitalize First
- `CapsLock` (press 3 times) → Toggle CapsLock

#### Other Functions
- `CapsLock + w` → Window Always on Top
- `CapsLock + m` → Minimize Window
- `CapsLock + s` → Open Perplexity
- `CapsLock + g` → Search Selected Text
- `CapsLock + r` → Enter Key
- `CapsLock + z` → Esc Key
- ``CapsLock + ` `` → Insert Date
- `CapsLock + F1` → Help Screen

## Common Use Cases

- **Document Editing**: Navigate through text and make edits without moving your hands from the home row
- **Coding**: Jump between words, delete lines, indent code all from keyboard center
- **Research**: Select text, search it online, and browse results without touching your mouse
- **Reference Work**: Keep important windows on top while checking other resources

## FAQ

**Q: Will this affect my normal CapsLock function?**  
A: The traditional CapsLock toggle is still available by pressing CapsLock three times quickly.

**Q: Does this work with all applications?**  
A: Most shortcuts work universally across applications. Some advanced functions may behave differently in specialized software.

**Q: What if I want to customize the shortcuts?**  
A: You can edit the `.ahk` script file with any text editor to modify the shortcuts according to your preferences.

**Q: What versions of Windows are supported?**  
A: This script works on Windows 7, 8, 10, and 11.

## How to Uninstall

1. Exit the script by right-clicking the "H" icon in the system tray and selecting "Exit"
2. Remove the script or its shortcut from your startup folder if you added it
3. Uninstall AutoHotkey if you no longer need it for other scripts

## Contributing

Contributions are welcome! If you have ideas for improvements:

1. Open an issue to discuss proposed changes
2. Fork the repository
3. Submit a pull request

## License

MIT License

---

# MagicCapsLock - CapsLock键盘魔法师：让CapsLock键化身超级生产力工具

[English](#magiccapslock-unlock-your-keyboards-hidden-magical-power) | [中文](#magiccapslock---capslock键盘魔法师让capslock键化身超级生产力工具)

**将几乎从不使用的 CapsLock 键变成效率倍增器**

你是否厌倦了：
- 不小心按到 CapsLock 后变成大写模式？
- 需要伸长手指去按那些遥远的快捷键？
- 在键盘和鼠标之间不断切换来导航文本？
- 重复执行相同的文本编辑操作？

这个轻量级脚本让你的指尖拥有30多个强大快捷键，无需改变键盘布局或学习复杂组合键。

**使用简单**：随时按下 CapsLock+F1 查看所有命令，无需死记硬背。

**零学习曲线**：从基础的导航功能开始，逐渐发现更多实用功能。

**保留原始功能**：连按三次 CapsLock 切换大小写锁定 - 这才是它该有的工作方式！

## 功能特点

### 核心功能
- CapsLock 成为 30+ 自定义快捷键的修饰键
- 连按三次 CapsLock 切换传统的大小写锁定状态

### 导航功能
- 类似 Vim 的光标移动（h/j/k/l 风格但使用不同按键）
- 快速的词/行/页面导航
- 高效的文本选择

### 文本编辑
- 轻松删除词、行和字符
- 大小写转换（小写、大写、首字母大写）
- 缩进控制

### 窗口管理
- 切换窗口置顶状态
- 最小化当前窗口

### 其他功能
- 快速 Google 搜索选中文本
- 快速访问 Perplexity AI
- 日期插入
- 内置帮助系统（Caps+F1）

## 快速上手指南

1. **安装 AutoHotkey**：
   - 从[AutoHotkey 官网](https://www.autohotkey.com/download/)下载
   - 运行安装程序，按照提示操作（默认选项即可）
   - 本脚本兼容 AutoHotkey v1.x 版本

2. **获取脚本**：
   - 从本仓库下载 `capslock_enhanced.ahk` 文件
   - 保存到计算机上的任意位置

3. **运行脚本**：
   - 双击脚本文件立即运行
   - 系统托盘会出现一个小"H"图标，表示脚本已激活

4. **随 Windows 自启动**（推荐）：
   - 按下 `Win+R`，输入 `shell:startup` 并回车
   - 将脚本复制或创建快捷方式到此文件夹
   - 这样每次启动计算机时脚本都会自动运行

## 使用方法

随时按下 **CapsLock + F1** 查看所有可用快捷键。

### 快捷键概览

#### 光标移动
- `CapsLock + f` → 向右
- `CapsLock + b` → 向左
- `CapsLock + u` → 向上
- `CapsLock + n` → 向下
- `CapsLock + p` → 向右移动一个词
- `CapsLock + o` → 向左移动一个词
- `CapsLock + a` → 行首
- `CapsLock + e` → 行尾
- `CapsLock + t` → 上一页
- `CapsLock + v` → 下一页

#### 文本编辑
- `CapsLock + h` → 退格
- `CapsLock + d` → 删除
- `CapsLock + k` → 删除右侧词
- `CapsLock + j` → 删除左侧词
- `CapsLock + l` → 删除至行尾
- `CapsLock + x` → 剪切整行
- `CapsLock + [/]` → 左/右缩进

#### 选择和格式化
- `CapsLock + q` → 选择模式
- `CapsLock + i` → 选择当前行
- `CapsLock + .` → 转为大写
- `CapsLock + ,` → 转为小写
- `CapsLock + /` → 首字母大写
- `CapsLock`（连按三次）→ 切换大小写锁定

#### 其他功能
- `CapsLock + w` → 窗口置顶/取消
- `CapsLock + m` → 最小化窗口
- `CapsLock + s` → 打开 Perplexity
- `CapsLock + g` → 搜索选中文本
- `CapsLock + r` → 回车键
- `CapsLock + z` → Esc 键
- ``CapsLock + ` `` → 插入日期
- `CapsLock + F1` → 帮助界面

## 常见使用场景

- **文档编辑**：无需将手移离键盘中心位置即可浏览和编辑文本
- **编程**：在单词间跳转、删除行、缩进代码等操作都可以从键盘中心完成
- **研究工作**：选择文本、在线搜索和浏览结果，无需触摸鼠标
- **参考工作**：保持重要窗口置顶，同时查看其他资源

## 常见问题

**问：这会影响我正常的 CapsLock 功能吗？**  
答：传统的 CapsLock 开关功能仍然可用，只需快速连按 CapsLock 三次即可。

**问：这在所有应用程序中都有效吗？**  
答：大多数快捷键在各种应用程序中都能通用。某些高级功能在特定软件中可能表现不同。

**问：如果我想自定义快捷键怎么办？**  
答：您可以用任何文本编辑器编辑 `.ahk` 脚本文件，根据您的偏好修改快捷键。

**问：支持哪些 Windows 版本？**  
答：该脚本适用于 Windows 7、8、10 和 11。

## 如何卸载

1. 右键点击系统托盘中的"H"图标，选择"Exit"退出脚本
2. 如果您已添加，请从启动文件夹中删除脚本或其快捷方式
3. 如果不再需要 AutoHotkey 用于其他脚本，可以卸载它

## 贡献

欢迎贡献！如果您有改进建议：

1. 打开一个 issue 讨论您的建议
2. Fork 仓库
3. 提交 pull request

## 许可证

MIT 许可证 